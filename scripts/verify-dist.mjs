import fs from 'node:fs'; import path from 'node:path';
const required=['index.html','en/index.html','it/index.html','en/medical-help-tourists-florence/index.html','it/assistenza-sanitaria-turisti-firenze/index.html','404.html'];for(const f of required)if(!fs.existsSync(path.join('dist',f)))throw new Error(`Missing dist/${f}`);
const files=[];const walk=d=>fs.readdirSync(d,{withFileTypes:true}).forEach(e=>e.isDirectory()?walk(path.join(d,e.name)):e.name.endsWith('.html')&&files.push(path.join(d,e.name)));walk('dist');for(const f of files){const html=fs.readFileSync(f,'utf8');for(const m of html.matchAll(/href="(\/[^"]+)"/g)){const href=m[1].split(/[?#]/)[0];if(/\.(svg|css|js|png|webp|avif)$/.test(href)||href==='/')continue;const target=path.join('dist',href,href.endsWith('.html')?'':'index.html');if(!fs.existsSync(target))throw new Error(`Broken internal link ${href} in ${f}`)}}console.log(`Verified ${files.length} HTML pages and primary static output.`);
