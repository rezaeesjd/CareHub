import {describe,it,expect,beforeEach} from 'vitest'; import {parseAttribution,persistAttribution,readAttribution} from '../src/lib/attribution'; import {whatsappUrl,canonical,phoneUrl,emailUrl} from '../src/lib/links';
class Memory{m=new Map<string,string>();getItem(k:string){return this.m.get(k)||null}setItem(k:string,v:string){this.m.set(k,v)}}
describe('privacy-safe attribution',()=>{let store:Memory;beforeEach(()=>store=new Memory());it('parses approved campaign fields and sanitizes referral',()=>{const a=parseAttribution('?ref=H001%3Cscript%3E&utm_source=hotel&utm_campaign=summer','/en/');expect(a.ref).toBe('H001script');expect(a.utmSource).toBe('hotel');expect(a.landingPage).toBe('/en/')});it('preserves first touch',()=>{persistAttribution(store,parseAttribution('?ref=H001'));persistAttribution(store,parseAttribution('?ref=B002'));expect(readAttribution(store)?.ref).toBe('H001')})});
describe('contact links',()=>{it('uses language-specific WhatsApp copy and referral',()=>{expect(decodeURIComponent(whatsappUrl('en','H001'))).toContain('Hello, I need healthcare assistance in Florence.\nReferral: H001');expect(decodeURIComponent(whatsappUrl('it'))).toContain('Buongiorno')});it('uses centralized contact details',()=>{expect(phoneUrl()).toBe('tel:+393758374492');expect(emailUrl()).toBe('mailto:florencecare24h@gmail.com')});it('builds canonical URLs from configured bases',()=>expect(canonical('/en/','https://care.example')).toBe('https://care.example/en/'))});
