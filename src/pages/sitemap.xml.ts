import type {APIRoute} from 'astro'; import {pages} from '../content/pages';
export const GET:APIRoute=()=>{const base=import.meta.env.PUBLIC_SITE_URL;if(!base)return new Response('',{status:404});const paths=['/en/','/it/',...pages.map(p=>`/${p.lang}/${p.slug}/`),'/en/privacy/','/it/privacy/','/en/cookies/','/it/cookie/','/en/terms/','/it/termini/','/en/professional-services/','/it/informazioni-servizi-professionali/','/en/accessibility/','/it/accessibilita/'];const xml=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${[...new Set(paths)].map(p=>`<url><loc>${new URL(p,base).toString()}</loc></url>`).join('')}</urlset>`;return new Response(xml,{headers:{'Content-Type':'application/xml'}})};
